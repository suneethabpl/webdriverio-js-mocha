export default class Page{
    open(path){
        browser.url(path);
    }
    // close(){
    //     //close the browser
    // }
    //note--we can create 
    //as many as methods as we want in our base class and then reuse it
    //in all of our classes using inheritance. 
    }
    